<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minyawy">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menues"></activity>
        <activity
            android:name=".Splash_Activity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlacesDescription" />
        <activity android:name=".Register_Activity" />
        <activity android:name=".Login" />
        <activity android:name=".Admin.RestaurantDescriptionAdmin" />
        <activity android:name=".PlaceListActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RestaurantDescription" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>